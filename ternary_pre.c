#include<stdio.h>
int main()
{ 
  int x,y,z;
  
  int max;
  
  printf("enter any three num : \n");
 // scanf("%d %d %d",&x ,&y,&z);
  
  int;
 x=6,y=50,z=400;
  max =(x>y && x>z) ? x:(y>z) ? y:z;
  printf("max num =%d", max);
  
  max =(x>y) ? (x>z) ?x:z :(y>z) ? y:z;
    
    printf("max out of %d ,%d and %d is : %d",x,y,z,max);
  
  
  return 0;
  
  
  
  
  
	
}